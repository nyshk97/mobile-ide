review session: 9b4b10ae-3d59-4bd8-8dea-93d041c834a8

## 1回目

````text
レビューしました。plan の主張のうち、コードで裏が取れるものは確認しています（`wireUp` の循環経路・`onDisappear` の現状・`console-run.py` の `--until` 実装・`verify-reconnect.py` の 5 シナリオ 8 項目・テストが TEST_HOST 付きでアプリのコードを使えること・`host-setup.sh` が `ClientAliveInterval 15 / CountMax 3` を持つこと）。SwiftTerm 側の API 可視性（`getTerminal()` / `updateUiClosed()`）は DerivedData と外部ネットワークに触れず未検証です。

## P0
- `Phase 1 > 3, 7` — FAIL / PASS の観測手段が `console-run.py` の実装と噛み合っていない。`--until` は一致した行で即 `done` → プロセスを kill するので「`--until "TERMINAL disconnected closed"` のあと 5 秒ログを見る」は実行不可能。PASS 側も「順序は問わない 2 行」を `--until "TERMINAL session deinit"` で待つと、先に session が出た時点で打ち切られ surface の行を取りこぼす。/ 両ゲートが「出なかった」を根拠にするので、観測できないだけの結果を修正の成否と取り違える。/ 2 行を待てる形にする（両方が出た後に必ず出る最終マーカーを 1 本足して `--until` はそれにする、または `--until` を付けず `--timeout` で流して行を集める。FAIL 側は `--until "TERMINAL session deinit" --timeout 40` で TIMEOUT になることを見る）。
- `Phase 1 > 1` — 目印行の指定が「`print` + `fflush`」と「`print` だけにする」で矛盾し、後者の理由（deinit が nonisolated だから）が誤り。`fflush(stdout)` は C 関数で isolation とは無関係、deinit から呼べる。/ `simctl launch --console` の stdout はパイプで全バッファリング。既存の目印行が全部 `fflush` しているのはそのため。落とすと deinit の行が窓の中に出ず P0 のゲートが必ず空振りする。/ deinit でも `print` + `fflush(stdout)` にする。
- `Phase 1 > 1, 3` — 目印行にインスタンス識別子が無い。`@State private var session = PTYSession()` / `surface: any TerminalSurface = SwiftTermSurface()` の初期値は `TerminalScreen` が初期化されるたびに生成され、SwiftUI が既存の箱を持っていれば捨てられる。捨てられた側も deinit するので、閉じる前に `TERMINAL surface deinit` が出うる。/ FAIL 側の判定条件が「deinit が出ないこと」なので、この 1 行で修正前でも「リークしていない」に見え、Phase 1 全体の根拠が崩れる。/ 目印行に `ObjectIdentifier(self)` を入れ、`wireUp` でも同じ id を 1 行出して「実際に使われた個体が死んだか」を突き合わせる。

## P1
- `Phase 1 > 4, 7` — PASS の条件が `SwiftTermSurface` と `PTYSession` の deinit だけで、`updateUiClosed()` が効いたか（＝`TerminalView` と CADisplayLink が解放されたか）を全く見ていない。`terminalDelegate` は weak・`SwiftTermSurface` は `TerminalView` を一方向に持つだけなので、DisplayLink が view を掴んだままでも surface の deinit は普通に出る。/ 前提 §1 で「残るのは DisplayLink」と書いた当の部分が未検証のまま PASS になる。/ 閉じる直前に `weak var` に `surface.view` を捕まえておき、数秒後に nil かどうかを 1 行出す（`TERMINAL view released=true|false`）。
- `Phase 1 > 4` — `updateUiClosed()` が SwiftTerm 1.20.0 で public か未確認のまま Phase 1 の中核に置かれている。Phase 2 には「見えなければ中止」の逃げ道があるが Phase 1 には無い。/ 非 public なら DisplayLink 側の対処手段が消え、Phase 1 の設計を書き直すことになる。/ 着手前に `getTerminal()` / `applicationCursor` / `updateUiClosed()` の 3 つと「再表示用の対（`updateUiOpened` 相当）」の有無を checkout の grep 1 回でまとめて確認し、パスごとログに残す。
- `Phase 1 > 5` — `wireUp` の `guard !wired` を残したまま `tearDown()` を足すと、同一 identity で `onDisappear` → `onAppear` が再来したときに復旧できない。/ `onInput` / `onResize` が nil で DisplayLink も止まっているので、画面は出るが打てず描画もされない（今の `session.close()` だけなら「再接続」ボタンで戻れる）。壊れ方が無言になる。/ `onDisappear` で `wired = false` に戻し、`wireUp` を再実行可能にする。
- `Phase 2 > 4` — RIS の検証を tmux 越しでやっても FAIL にならない見込み。tmux は `ESC c` を自分のペインに対して解釈し、クライアント（アプリ）へは自分の判断で `ESC[?1l`（smkx/rmkx）を送り直す。VERIFY.md にも「tmux が矢印をペインのモードに合わせて再送する」と既に書いてある。/ 自前パーサも tmux が送る `ESC[?1l` を拾ってしまうので、「修正前は true のまま」という前提が崩れ、意味のない PASS を根拠にする。/ `MobileIDETests`（`TEST_HOST` 付きでアプリのコードと UIKit が使える）に `SwiftTermSurface` へ直接 feed する検査を足す: `ESC[?1h` → true、`ESC c` → false、チャンク分割で `ESC[?` / `1h` に割っても true。tmux 越しの手順はオン / オフの回帰確認だけに使う。
- `Phase 3 > 2` — `backgroundedAt` を `.running` 分岐でしか消さない。バックグラウンド中に接続が落ちて `.reconnecting` になっていると、復帰時の `verifyAlive()` は `retryNow()` 経路に行き `backgroundedAt` が残る。/ その後 `running` に戻った接続で `network.onChange` が発火したとき、何分も前の記録を読んで生きている接続を無条件に切る。/ `verifyAlive()` の入口（state の switch より前）で読み取りと同時にクリアする。
- `Phase 3 前の計測 > 2` — 計測に `MOBILE_IDE_OPEN_PROJECT=mobile-ide` を使う。attach は `new-session -A -D` なので、Mac 側で `mobile-ide` に attach している Claude Code のクライアントを蹴る。VERIFY.md がこの事故（2026-09-05 の実例）を理由に自走検証では `form` を使えと明記している。/ 実機を繋いだ瞬間に作業中のセッションが detach され、Phase 4 の「`list-clients` が 1 件」も前提が崩れる。/ 計測も専用セッション（`form` など）を使うか、Mac 側の attach が無いことを確認してから始める旨をステップに書く。
- `Phase 3 前の計測 > 4` — 実機 1 回・90 秒の観測で Phase 3 の要否を決め、判定を「`probe dead` が出るか / `lost connection closed` が先に出るか」の順序に置いている。復帰時は Citadel の `onDisconnect` と `verifyAlive()` が同時に走り、どちらが先かは決まらない。/ たまたま RST が届いた 1 回で「該当せず」と結論すると、モバイル回線・長時間サスペンド側の本命ケースを取りこぼしたまま Phase 3 を落とす。/ 90 秒・5 分の 2 条件、Wi-Fi とモバイル回線の 2 経路で最低 2 回ずつ測り、判定は順序ではなく「復帰 → `connected` の秒数」で見る。

## P2
- `Phase 1 > 8` / `Phase 3 > 6, 8` — シナリオ番号が衝突する。Phase 1 で 6 シナリオ 10 項目になった後に Phase 3 が「6」「7」を足し、最終を「7 シナリオ」と書いている（実際は 8 シナリオ 12 項目）。VERIFY.md の「5 シナリオ 8 項目」表記も 2 か所ある。/ 番号を通しで振り直し、最終数を 1 か所に決めてから両ファイルを直す。
- `Phase 3 > 2, 6` — `lost("stale after background \(秒)s")` の秒の書式が未指定。`Duration` や経過時間をそのまま補間すると `90.000...s` になり、シナリオ 6 の文字列一致が外れる。/ 整数に丸めて出す（`Int(elapsed.components.seconds)`）。
- `Phase 2 > 6` — 追記先が `docs/plans/2026-09-04-1838-keyboard-bar.md:110` だが、該当する記述（「`getTerminal()` は無く…」）は「試したこと・わかったこと」の 2026-09-04 の行と「方針変更」の 2 か所にある。/ 行番号でなく節名で指し、両方に追記する。あわせて今回読んだ checkout の実パスも書く（前回と今回で結論が逆なので、3 度目に同じ調べ直しをしないため）。

## Q
- `Phase 3 > 1` — `probeTimeout` そのものを短く（3 秒 → 1 秒程度）する案を採らないのか、閾値方式と併用するのか。/ 短縮で足りるなら Phase 3 の実装・起動オプション・シナリオ 2 本がまるごと不要になる。逆にモバイル回線で生きている接続を誤判定するリスクは上がる。どちらを選ぶかで Phase 3 の形が変わるため、Phase 3 前の計測の前に決めておきたい。
- `Phase 3 > 1` — 60 秒の根拠であるホスト側 `ClientAliveInterval 15` / `CountMax 3` は `scripts/host-setup.sh:43` でこのリポジトリが書いている値。これを維持する前提でアプリ側に閾値を置くのか、ホスト側を緩めて「そもそも切られない」方向に倒すのか。/ 決めずに定数だけ入れると、host-setup.sh を変えた瞬間に `staleAfterBackground` が黙って嘘になる（切れた sshd-session と tmux クライアントの掃除という当初の目的とのトレードオフでもある）。維持するなら定数の doc コメントから `scripts/host-setup.sh` を名指しで参照させたい。

````

**対応**: P0 3 件すべて反映。(1) 観測手段: `console-run.py --until NEVERMATCH --timeout N` で全目印行を流す形に改め（`verify-reconnect.py` の `App` と同じ）、FAIL/PASS の条件を書き直した。(2) 目印行は全部 `print` + `fflush(stdout)` に統一し、誤った理由付けを削除。(3) `wireUp` で surface/session の `ObjectIdentifier` を出し、同じ id の deinit で判定する形に。P1: view の解放を `weak` 参照で `TERMINAL view released=` として見る（既存ゲートの条件の書き換えとして採用。surface deinit だけでは `updateUiClosed()` の効きが見えないため）／`updateUiClosed()` は checkout で public を確認済み、開き直す対が無いことも前提に追記し、着手前の grep 再確認ステップを Phase 1 先頭に置いた／`onDisappear` で `wired = false` と tearDown 済み surface の再利用を print で検出／RIS は tmux 越しでは検証不能と前提に書き、`SwiftTermSurfaceTests`（直接 feed、RIS・チャンク境界）に置き換え、修正前 FAIL を取る順にした／`backgroundedAt` は `verifyAlive()` 入口でクリア／計測は `form` セッション・90 秒と 5 分 × Wi-Fi とモバイルの計 4 回・判定は順序でなく秒数に。P2: シナリオ番号を 6（解放）・7・8 に通し、最終数を「8 シナリオ 14 項目（実装時に確定、2 か所同じ数に）」に／秒は `Int(elapsed.components.seconds)`／keyboard-bar plan への追記先を節名で 2 か所に、checkout の実パスも書くように。Q: 2 件とも自分で決めて plan に記載。`probeTimeout` 短縮ではなく閾値方式（短縮でも 1 秒は払い、モバイル回線で誤判定リスク。根拠: 前提 §3 の設計と `-D` の無害性）／ホスト側 ClientAlive は維持し、`staleAfterBackground` の doc から `scripts/host-setup.sh` を名指し参照（根拠: VERIFY.md ホスト節の掃除目的）。

## 2回目

````text
plan を読み直しました。P0 3 件と P1 の対応は、`console-run.py --until NEVERMATCH` が `verify-reconnect.py` の `App` と同じ形であること、`updateUiClosed()` に開き直す対が無い前提、`backgroundedAt` の入口クリア、件数の通し番号（11 + 3 = 14）まで含めて筋が通っています。残りは以下です。

## P0
- `Phase 2 > 1` / `Phase 3 > 2` — 修正を入れるステップが、その修正の「修正前 FAIL」を取るステップより前に並んでいる。Phase 2 は step 1 で自前パーサを消した後、step 4 で「テストを先に書いて FAIL を取ってから置き換える」と言う。Phase 3 も step 2 で `verifyAlive()` ごと変えた後、step 5 で「`verifyAlive` は未変更の状態で FAIL を取る」と言う。/ 前提「共通」と CLAUDE.md が「検査を足したら修正前に FAIL することを先に見る」を明示しているのに、チェックリストの順に実行すると必ず stash / revert して取り直すことになる。Phase 1 だけ正しい順（目印行 → FAIL → 修正）になっているので、順序の不統一が事故になりやすい。/ Phase 2 は `SwiftTermSurfaceTests` を step 1 に、`mise run test` で RIS の 1 件が FAIL することを step 2 に、置き換えを step 3 以降に。Phase 3 は `enterBackground(at:)` + 起動オプション → 修正前 FAIL → `verifyAlive()` の閾値判定、の順に並べ替える。

## P1
- `Phase 1 > 5, 9` — 手打ちの `console-run.py` に `MOBILE_IDE_HOST` / `MOBILE_IDE_USER` が無い。VERIFY.md の手順も `verify-reconnect.py` の `CONN_ENV` も必ずこの 2 つを渡している。/ 接続設定は環境変数の上書きが無ければ UserDefaults 頼りで、未保存のシミュレータでは「接続先を設定してください」の一覧で止まる。25 秒待った末に「deinit が出ない」だけが観測され、FAIL ゲートを通ったように見えてしまう（`TERMINAL wired` が出ないことで気づけるが 1 サイクル無駄になる）。/ コマンドに `--env MOBILE_IDE_HOST=127.0.0.1 --env MOBILE_IDE_USER=d0ne1s` を足す。
- `Phase 1 > 10` — 自走シナリオ 6 が「`App.wait` で 3 行を待つ」だけで、id の突き合わせをしない。`App.wait` / `has` は単なる部分一致なので、`TERMINAL surface deinit <id>` が出さえすれば PASS になる。/ step 2 で id を入れた理由（捨てられた `@State` 初期値の deinit と区別する）が自走側では失われ、手で見たときだけ厳しく機械には緩い検査になる。回帰を拾えない。/ シナリオ 6 で `app.lines` から `TERMINAL wired surface=<id> session=<id>` を正規表現で取り出し、その 2 つの id を含む deinit 行がそろうことを条件にする。
- `Phase 2 > 4` — `SwiftTermSurfaceTests` が `SwiftTermSurface()` をそのまま作ると `TerminalView(frame: .zero)` のままレイアウトが走らず、桁数・行数が 0 の状態で feed することになる。/ SwiftTerm がどうクランプするか次第で、パーサが動かず 4 件とも意味のない結果になりうる。アプリ本体は SwiftUI が後からレイアウトするので同じ問題が出ず、テストだけ落ちて原因が分かりにくい。/ テスト用に非ゼロの frame を与えられるようにして（`SwiftTermSurface` の init に frame を渡すか、テスト側で `surface.view.frame = CGRect(x:0,y:0,width:400,height:400)` + `layoutIfNeeded()`）、`currentSize` が非 nil になってから feed する。
- `Phase 3 前の計測 > 5` — 復帰の起点にする目印行が存在しない。「復帰（`NETWORK path` か最初の目印行）」とあるが、`onChange(of: scenePhase)` は今も何も print せず、`NETWORK path` は経路のキーが変わったときしか出ない。生きている接続で戻れば `probe ok` の 1 行しか出ず、起点が無いので秒数を測れない。/ 判定を「順序でなく秒数」に変えた当の測り方が成立せず、代替手段（人間が声で合図）に落ちて精度が出ない。Phase 4 の「計測の表と同じ条件で 3 秒以上縮む」も同じ理由で測れない。/ `onChange(of: scenePhase)` に `TERMINAL foreground` / `TERMINAL background`（`print` + `fflush`）を足すステップを Phase 3 前の計測の先頭に置く。ついでに Phase 3 > 3 の `enterBackground()` が実機で本当に呼ばれたかの確認にもなる。

## P2
- `Phase 1 > 3` — `view released` を出す 2 秒の Task が、自走かどうかに関わらず端末を閉じるたびに本番経路で走る。/ 他の目印行は状態遷移のついでだが、これは検証専用の待ちを 1 本増やす。`#if DEBUG` か `LaunchOptions.closeAfter != nil` のときだけにしておくと、後から「これは何のための遅延か」と読み直さずに済む。
- `Phase 1 > 7` — 「`wireUp` は tearDown 済みの surface を掴んだら print」の実現手段が未指定。`TerminalSurface` に足すのは `tearDown()` だけなので、画面側からは tearDown 済みか判別できない。/ 判別用の状態を protocol に足すか、`SwiftTermSurface` 側（tearDown 後に `onInput` が設定されたら print）に閉じ込めるかを決めておく。後者なら protocol を汚さない。
- `Phase 5 > 1` — 件数の書き換え先の数え方がずれている。「5 シナリオ 8 項目」の文字列は VERIFY.md 再接続節の 1 か所だけで、もう 1 か所は画像添付節の「`verify-reconnect.py` 8 / 8 も壊れていないこと」。加えて `verify-reconnect.py` 冒頭の docstring に「検証項目: 1〜5」の一覧がある。/ 「VERIFY.md 2 か所 + docstring の一覧」と具体名で書いておくと漏れない（Phase 3 > 6 の「2 か所」とも数が合う）。
- `Phase 4 > 4` — 人間のみのステップになっているが、秒数を測るには Phase 3 前の計測と同じ devicectl のログ取得が要る。/ AI がログを流す前提を書くか、ここは「バナーが一瞬で消える」の体感確認に留めて秒数比較は Phase 3 前の計測の再実行に寄せる。

## Q

````

**対応**: P0: Phase 2 と Phase 3 の順序を「検査を足す → 修正前 FAIL → 修正 → PASS」に並べ替えた（Phase 2: SwiftTermSurfaceTests を先に足して RIS の 1 件が FAIL するのを見てから置き換え。Phase 3: enterBackground(at:) + 起動オプション + シナリオ 7/8 を先に入れ、verify-reconnect.py でシナリオ 7 が FAIL するのを見てから閾値判定を入れる）。P1: 手打ちコマンドに MOBILE_IDE_HOST / MOBILE_IDE_USER を足した／シナリオ 6 は wired 行から正規表現で id を取り、その id を含む deinit 行で判定するように書き換えた／SwiftTermSurfaceTests は非ゼロの frame を与えて layoutIfNeeded し currentSize が非 nil になってから feed すると明記／Phase 3 前の計測の先頭に TERMINAL foreground / background の目印行を足すステップを置き、秒数の起点を foreground 行に変えた（Phase 4 も同じ起点）。P2: view released の 2 秒待ちは #if DEBUG に閉じる／tearDown 済み再利用の検出は SwiftTermSurface 側に閉じ protocol は tearDown() だけ／件数の書き換え先を「VERIFY.md 再接続節・画像添付節の 2 か所 + docstring の一覧」と具体名に／Phase 4 の秒数比較は Phase 3 前の計測の再実行（AI がログを流す）に寄せ、人間のみのステップは体感確認に留めた。見送りなし。

## 3回目

````text
plan を読み直しました。P0 の並べ替え（Phase 2: テスト → FAIL → 置き換え、Phase 3: 記録側 + 起動オプション + シナリオ → FAIL → 閾値判定）、id 突き合わせのシナリオ化、非ゼロ frame、`TERMINAL foreground` / `background` の追加は、いずれも指摘の意図どおりに入っています。手戻りが確定する箇所は残っていません。

## P0

## P1
- `Phase 3 前の計測 > 4` — 実機の `console-run.py` に接続先の指定が無く、保存済み設定に依存している。VERIFY.md の実機手順は `--env MOBILE_IDE_HOST=tsubasamacbook-air.tail9fb38b.ts.net --env MOBILE_IDE_USER=d0ne1s` を明示して渡している。/ この計測は「モバイル回線でも同じ秒数か」を見るのが目的なので、接続先が Tailscale 名でなく `.local` 等だとモバイル回線の 2 回が「そもそも繋がらない」になり、人間を拘束したまま条件が 1 つ潰れる。設定が何だったかもログに残らない。/ 4 条件の意味が確定するよう、HOST / USER をコマンドに明示する（表にも接続先を 1 列書く）。
- `Phase 3 前の計測 > 5` — モバイル回線の 2 回をどう作るかが書かれていない。実機で回線をモバイルにするには Wi-Fi を切ることになるが、devicectl のトンネルは Wi-Fi 経由だと `unavailable` になりがち（VERIFY.md 実機節）で、Wi-Fi を切った瞬間にログが途切れる。さらに Wi-Fi の切り替え自体が `NWPathMonitor` を動かして `network.onChange` → `verifyAlive()` を呼ぶ。/ ログが取れなければ `TERMINAL foreground` を起点にする測り方が成立せず、代替（声で合図）に落ちる。回線を切り替えた直後にバックグラウンドへ入れると、probe や再接続が計測開始前に走って秒数が汚れる。/ モバイル回線の回は USB 接続で取ると条件に書き、「回線を切り替える → `probe ok` / `connected` が落ち着くのを待つ → それからホームに戻す」の順を手順に入れる。

## P2
- `Phase 2 > 1` — `ESC [ ? 1 l` → false のケースが、`usesApplicationCursorKeys` の初期値と同じなので何も feed しなくても通る（修正前も修正後も落ちない検査）。/ 同じ surface に `ESC [ ? 1 h` → true を確認してから `ESC [ ? 1 l` → false を見る形にすると、オフ方向の遷移が実際に検査される。
- `Phase 3 前の計測 > 1` — `onChange(of: scenePhase)` は `.inactive` ↔ `.active` の往復（通知センター、着信バナー、コントロールセンター）でも `.active` を通るので、バックグラウンドを挟まない `TERMINAL foreground` が出る。/ 秒数の起点は「`TERMINAL background` と対になった `foreground`」に限る、と表の作り方に 1 行書いておくと、計測中の余計な往復を後から捨てられる。
- `Phase 1 > 3` — 2 秒という待ちの根拠が無く、`view released=false` は「surface がまだ生きている（`@State` の箱の解放が遅れた）」と「view が DisplayLink に掴まれている」を区別しない。/ PASS 条件は deinit 2 本と併せて見るので誤 PASS はしないが、落ちたときに原因を取り違えやすい。`released=false` かつ `surface deinit` も出ていなければ待ちを伸ばして見直す、と切り分けを 1 行添えておく。
- `Phase 1 > 9` — `session deinit` が出なかったときの一次容疑がクロージャの循環とは限らない。`launchAttempt` の Task は `withPTY` のクロージャ内で `self` を強く掴んでおり、`close()` の `cancel()` と `client.close()` で inbound が終わるまで `PTYSession` は生き続ける。/ 前提 §1 は循環だけを原因として書いているので、PASS が出ないときに配線の nil 化を疑って空回りしうる。「deinit が遅れて出るなら Task の終了待ち、いつまでも出ないなら循環」と切り分けを 1 行書いておく。

## Q

````

**対応**: P0 なしで収束。P1/P2 はすべて既存ステップの書き換えで済むので採用: 実機計測のコマンドに Tailscale 名の HOST / USER を明示し表に接続先の列を足す／実機は USB で繋ぎ、モバイル回線の回は「Wi-Fi を切る → 落ち着くのを待つ → ホームへ」の順を手順に／`?1l` → false はオンにしてから見る／foreground の起点は background と対のものだけ／view released=false と session deinit 遅延の切り分けを 1 行ずつ添えた。見送りなし。
