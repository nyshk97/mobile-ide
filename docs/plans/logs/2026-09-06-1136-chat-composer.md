review session: 944736e1-cdcb-42a6-a8cd-e2ff071dac95

## 1回目

````text
`docs/plans/2026-09-06-1131-chat-composer.md` と既存実装（`TerminalScreen` / `SwiftTermSurface` / `WheelScrollingTerminalView` / `AttachmentFlow` / `PTYSession`）を突き合わせてレビューしました。

## P0
- `設計 > 3`（ComposerMessage）/ `調査で確定した事実 > 3` — 実測は tmux `paste-buffer` 経由なのに、実装は `TextEditor` の LF をそのまま包む。`paste-buffer` は既定で **LF を CR に置換**してから貼る（`-r` を付けたときだけ LF のまま）ので、「Codex/Claude が 3 行を 1 入力として受けた」という確定事実は `ESC[200~ line1 \r line2 \r line3 ESC[201~` の話であって、LF 区切りで同じになる保証がない / TUI 側で行が繋がる・改行が落ちる形で外れる。`bytes(for:)` で本文の `\n` を `\r` に変換して包む（tmux と同じ）ことを設計に明記し、`ComposerMessageTests` に区切りが `\r` であるケースを入れる。LF のまま送りたいなら Phase 1 前に Air で両方を実測して決める。
- `設計 > 1,2` / `Phase 3 > 2` — `bracketedPasteMode` を「SwiftTerm が持っている」前提だが、経路は tmux 越しであり、ペインのモードがクライアント端末へ転送されるかは tmux の版と TERM（`xterm-256color`）依存（tmux 3.3+ は `Enbp`/`Dsbp` 経由）。転送されなければ検出は永久に false のままで、複数行が素のまま流れて **1 行ごとに Claude/Codex に送信される** = この機能で一番避けたい壊れ方が本番だけで起きる。Phase 1 の前に、tmux 内で claude を起こしてアプリ（または素の ssh）側に `ESC[?2004h` が届くかを 1 回確認する手順を「事前準備」に入れる。あわせて `bytes(for:)` に「`bracketedPaste == false` かつ複数行なら送らない（アラート or 直接入力を促す）」ガードを入れる — 素で流す fallback は無害ではない。
- `設計 > 6` / `Phase 2 > 1` — `TextEditor` では決定事項が実装できない。(a) カーソル位置への挿入 API が無い（`TextEditor(text:selection:)` は iOS 18、本プロジェクトは deploymentTarget 17.0）→ 決定事項 8「`@パス ` をカーソル位置に差し込む」とスラッシュ挿入が満たせない、(b) IME の未確定文字を送信前に確定する手段が無い → `Phase 4 > 1`「未確定のひらがなが残らない」が担保できない、(c) placeholder が無い。最初から `UIViewRepresentable` の `UITextView`（`selectedRange` で挿入、送信前に `markedTextRange` を見て `unmarkText()`、`sizeThatFits` で 1〜6 行）で作る。Phase 2 まで進んでから差し替えると ComposerView とその配線を作り直しになる。
- `設計 > 8` / `Phase 2 > 3` — 「composer モードでは端末 view にフォーカスを渡さない」を実現する仕掛けが無い。マウス追跡が無い場面（素のシェル・`cat -v`・`less`）では `shouldForwardUnfocusedTap` が false になり SwiftTerm の singleTap が `becomeFirstResponder()` するので、**端末を 1 回タップした時点でキーボードが端末に移り、以後の打鍵が composer を素通りして PTY に直行する**（下書きは残ったまま二重入力になる）。`TerminalSurface` に入力可否のフラグを足して `WheelScrollingTerminalView.canBecomeFirstResponder` を false にする（`shouldForwardUnfocusedTap` の条件も合わせて見直す）。`Phase 3` に「composer モードで端末をタップしてもキーボードが移らない」を 1 ステップ追加。
- `Phase 3 > 3,4` / `設計 > 10` — `MOBILE_IDE_COMPOSE` を「接続後 1 秒」に固定すると、`MOBILE_IDE_PRESS_KEYS=claude` と併用するこの 2 ステップが回らない。`runAutomation` は type → pressKeys → probe … の直列で、Claude / Codex は起動に数秒かかるため、COMPOSE は pressKeys の**後**に置き、`MOBILE_IDE_COMPOSE_AFTER=<秒>`（既定 1）を足す必要がある。

## P1
- `設計 > 3` — 本文に `ESC` / `ESC[201~` が含まれると包みを途中で閉じられる（端末ログや差分を貼るのは普通に起きる）。`bytes(for:)` で `ESC[201~` を落とす（または `ESC` を除去する）ことを決め、テストに 1 ケース足す。
- `Phase 2 > 3` — 入力欄が 1→6 行に伸びるたびに端末の rows が変わり、`PTYSession.resize` はデバウンス無しで即 `changeSize` を送る。改行を打つたびに tmux / Claude が全面再描画してちらつく。行数変化の resize を ~200ms デバウンスするか、入力欄を端末に重ねて rows を変えない設計にする。
- `Phase 3 > 3` / `Phase 4 > 1` — 合格条件「capture-pane で本文が『送信済み』の位置に出る」は、Claude Code が複数行の貼り付けを `[Pasted text #1 +N lines]` に畳むと満たせず、正しく動いていても FAIL に見える。合格条件を「畳まれた表示でも本文の全行が 1 入力として渡ったと分かること」に書き換える。
- `設計 > 9` — `AttachmentFlow` を `onTyped` closure 化するとき、末尾の `session.state == .running` 分岐の意味が composer では変わる（切断中でも入力欄には挿せるので「端末に入力できませんでした」アラートは不要、むしろ挿すのが正しい）。「挿す先が composer か」を渡すか、状態判定を呼び出し側に出す。
- `Phase 3 > 7` — 下書き復元の確認に使える「送信せず下書きだけ入れる」入口が無い（`MOBILE_IDE_COMPOSE` は送信までする）。`MOBILE_IDE_DRAFT=<text>` を足すか、COMPOSE に送信しないモードを付ける。
- `設計 > 目印行` — `Phase 3 > 7` が使う `COMPOSE draft restored n=<文字数>` が目印行の一覧に無い。一覧に足す（実装漏れになりやすい）。

## P2
- `設計 > 5` — `onChange` ごとの UserDefaults 書き込みは打鍵ごとの同期書き込みになる。デバウンスするか、`onDisappear` / `scenePhase` 変化で保存する。
- `設計 > 6` — 起動直後の入力欄の自動フォーカスは、既存の `DispatchQueue.main.async { surface.showKeyboard() }` と同じ理由（push アニメーション中に外れる）で 1 tick 遅らせる必要が出やすい。
- `設計 > 5` — プロジェクトを消しても `composer.draft.<session>` が UserDefaults に残る。掃除の当てを一言書いておく。
- `Phase 3 > 8` — VERIFY.md に加えて README の入力まわりの説明も更新対象（直前のコミットで MVP の現状に合わせたばかりなので、ここだけ古くなる）。

## Q
- `決定事項 > 1` / `設計 > 8` — composer モードで端末をタップしたときの扱い: フォーカスを渡さないだけにするか、直接入力モードへ自動で切り替えるか。決めないと P0（first responder の封じ方）の実装とバーの切替ボタンの意味が二転する。
- `概要` — 送信した本文の控えをアプリ側に残すか。Claude Code は貼り付けを畳むので、送信後は端末にも入力欄にも本文が残らず、「送る前に読み返せる」ようにしたのに送った後は読み返せない状態になる。残すなら composer に送信履歴（直前の本文を戻す操作）が要り、後付けだと入力欄の UI をもう一度触ることになる。

````

**対応**: P0 は全部反映。(1) LF→CR 変換を `bytes(for:)` の仕様とテストに追加（tmux `paste-buffer` の LF→CR 置換を事実として明記）。(2) tmux 越しの bracketed paste 転送を Air で pty から実測: tmux 3.7c はクライアントに常に `?2004h` を送り、クライアントからの包みをペインのモードに合わせて剥がす / 通す。これで `usesBracketedPaste` を読む設計自体を廃止し「常に包む」に単純化（フォールバックのガードも不要になった。事実・決定事項・設計・Phase 1・Phase 3 を書き換え）。(3) `TextEditor` → `UITextView`（UIViewRepresentable）。(4) `acceptsKeyboard` フラグで `canBecomeFirstResponder` を制御、Phase 3 と Phase 4 にタップの確認を追加。(5) `MOBILE_IDE_COMPOSE_AFTER` を足し pressKeys の後に発火。P1: ESC の除去を仕様とテストに追加 / Claude の合格条件を「1 回の送信として受けた（畳み表示可）」に書き換え / AttachmentFlow の composer 時は接続状態を見ずに挿入 / `MOBILE_IDE_DRAFT` を追加（Phase 3 の下書き確認がこれ無しでは回らないため）/ 目印行一覧に `draft restored` を追加。P1 の resize デバウンス（1→6 行の伸びで rows が変わりちらつく）は仕組みの追加なのでループ中は見送り、終了報告へ。P2: 下書き保存を送信時 / onDisappear / background に変更 / 古い下書きは掃除しないと明記 / README の更新を Phase 3 に追記。focus の 1 tick 遅延は実装時の細部なので書かない。Q は両方こちらで決定: 端末タップはフォーカスを渡さないだけ（自動切替なし、誤タップ対策）/ 送信履歴は今回の範囲外（Claude Code の会話ログに残る）。決定事項に追記。

## 2回目

````text
plan を読み直し、前回の指摘の反映状況と新しく生じた点を、実装（`WheelScrollingTerminalView` / `TerminalScreen` / `LaunchOptions` / CLAUDE.md）と突き合わせて見ました。P0 は 5 件とも設計・事実・Phase に反映されており、残りは実装前に足すと効く細部です。

## P0

## P1
- `設計 > 1,2`（TerminalSurface / SwiftTermSurface）— `WheelScrollingTerminalView.canBecomeFirstResponder` を override する前提だが、SwiftTerm 1.20 が `public override var`（`open` でない）で宣言していると別モジュールのサブクラスからは override できずコンパイルが通らない。Phase 1 の 1 行目で詰まる。Phase 1 の最初に 1 行だけ書いてビルドし、通らなければ `becomeFirstResponder()` を override して composer 時は false を返す（UIResponder 側のメソッドなので宣言に縛られにくい）か、`UnfocusedClickGestureRecognizer` の `shouldRecognize` を「マウス追跡中 or composer モード」に広げてタップ自体を吸う、へ切り替える。どちらを取るかで `acceptsKeyboard` の置き場所が変わるので先に決める。
- `設計 > 6`（ComposerView）— `UIViewRepresentable` の `updateUIView` で `uiView.text` を無条件に書き戻すと、日本語の変換中に未確定文字が消える / カーソルが先頭に飛ぶ（SwiftUI 側の binding 更新と IME のマーク状態がぶつかる定番の壊れ方）。`Phase 4 > 1`「未確定のひらがなが残らない」の合格条件そのものが、送信直前の `unmarkText()` だけでは守れない。仕様に「`markedTextRange != nil` の間と値が同じときは書き戻さない」を 1 行足す。
- `Phase 3 > 5` — `simctl` にはタップを合成するコマンドが無い（`io` は screenshot / recordVideo のみ、`ui` は外観設定）ので、この手順は今の自走の枠組みでは実行できない。さらに判定材料の「`TERMINAL size` の rows が変わらない」は、端末にフォーカスが移ってもキーボードは出たままなので rows は変わらず、壊れていても PASS する。DEBUG の起動オプション（例 `MOBILE_IDE_TAP_TERMINAL=<秒>` で端末中央のタップ経路を呼ぶ）を足し、判定は新しい目印行 `COMPOSE focus=<composer|terminal|none>` で見る。
- `Phase 3 > 9` / `設計 > 目印行` — モードの「保存 → 上書き無しの通常起動で復元」を自走が一度も通らない（step 9 は切替時の目印行を見るだけ、step 6 は `MOBILE_IDE_INPUT_MODE` の上書き起動）。CLAUDE.md に記録済みの事故（上書き注入ばかりで保存経路を踏まず、実機の通常起動で初めて壊れが出た）と同じ形。「`inputMode` で direct にして `MOBILE_IDE_CLOSE_AFTER` で閉じ、上書き無しで `OPEN_TIMES=2` の 2 回目が direct で開く」を足し、`COMPOSE mode=` を切替時だけでなく復元時にも出す。
- `決定事項 > 9`（端末タップ）/ `設計 > 1` — 端末が first responder になれないと、長押しの選択メニュー（Copy）も出せなくなる公算が大きい（編集メニューは first responder が前提）。端末に出たエラー文をコピーして入力欄に貼る、という一番ありそうな動線が composer モードで死ぬ。今その操作ができるかを 1 回確かめ、割り切るなら「端末のコピーは直接入力に切り替えてから」を決定事項に明記、残すなら選択中だけ first responder を許す仕掛けを設計に入れる。

## P2
- `決定事項 > 4` と `設計 > 3` — 決定事項は「**複数行は**常に bracketed paste で包む」、設計は「1 行でも常に包む」。実装者が決定事項だけ読むと複数行だけ包む実装になる。決定事項の文言を「1 行でも常に包む」に合わせる。
- `調査で確定した事実 > 2` — 包みを剥がす / 通す挙動は tmux の版に依存する（実測は Air の 3.7c）。接続先は Mac mini に移るので、VERIFY.md 側に版の確認と `printf "\e[?2004l"; cat -v` の 1 行手順を残しておくと、mini で挙動が違ったときに切り分けが 1 分で済む。
- `Phase 2 > 6` — シミュレータはハードウェアキーボード接続が既定でオンだとソフトウェアキーボードが出ないので、`mise run shot` の「キーボードを出したとき端末が縮んで入力欄が見える」が撮れないことがある。撮れない場合の前提（I/O > Keyboard の設定）を手順に添える。

## Q

````

**対応**: P0 なしで収束。P1 は 5 件とも書き換えで反映: (1) SwiftTerm 1.20 の `canBecomeFirstResponder` は `open override` と確認（override 可）。ただし (5) の指摘どおり塞ぐと長押し → Copy（`longPress` が `becomeFirstResponder()` してから `UIMenuController`）が死ぬので、設計を「`inputView` を高さ 0 の view に差し替えてキーボードだけ出さない」に変更（`acceptsKeyboard` → `showsKeyboard`）。端末タップで入力欄のキーボードが閉じる挙動を決定事項に追記。(2) `updateUIView` は `markedTextRange != nil` の間と同値のときは書き戻さない、を設計に 1 行追加。(3) `simctl` で撃てない Phase 3 のタップ確認を削除し、実機（Phase 4）で長押し → Copy まで見る形に書き換え。VERIFY に「自走では撃たない」と明記する旨を追記。(4) モードの保存 → 上書き無しの通常起動で復元、を Phase 3 に追加し `COMPOSE mode=` を復元時にも出す（CLAUDE.md の事故の形）。P2: 決定事項の「複数行は」→「1 行でも常に」に統一 / VERIFY に tmux の版と `?2004l; cat -v` の 1 行手順を残す旨を追記 / シミュレータのハードウェアキーボード設定の注意を Phase 2 に追記。
