review session: 8a320854-17c5-41ef-a30a-9e384dce3a72

## 1回目

````text
plan と実コード（`PTYSession`, `SSHConnection`, `ConnectionSettings`, `scripts/ssh-proxy.py`, `TerminalTarget`）を突き合わせてレビューしました。

## P0
- `調査で確定した事実 > 5`（`@` メンション）/ `Phase 3 > 7` — 検証が非対話の `claude -p` だけで、実際の経路（tmux 上の対話 TUI に `PTYSession.send` で 1 文字列を流し込む）を一度も通していない / Claude Code の TUI は `@` でファイル補完のオーバーレイが開き、続く文字はその絞り込みに食われる。`claude -p` が通っても対話で通る保証にならず、ここが崩れると「端末に流し込む」という feature の中核 UI（AttachmentButton の後段・目印行 `UPLOAD typed`）を作り直すことになる / 事前準備に 1 項目足して、ホストで `tmux new -d -s probe 'claude'` → `tmux send-keys -t probe '@/Users/d0ne1s/.claude/uploads/x.jpg '` → `capture-pane` でプロンプトに素のパスとして入るかを先に見る。補完に食われるなら「先頭に空白を置く」「`Read <path>` を送る」など送る文字列の形をここで決めてから Phase 1 に入る。あわせて `決定事項` 側の「Phase 4 で確かめる」と `Phase 3 > 7` の記述の食い違いも直す
- `Phase 3 > 6`（失敗経路）— `MOBILE_IDE_PORT=2222` で中継を止めたままにすると、端末の接続も同じ `ConnectionSettings` を使うので `running` に到達せず、`MOBILE_IDE_UPLOAD_FILE` の自走（`runAutomation` は running 契機）自体が発火しない / 「端末側は影響を受けず接続したまま」という期待は現在の実装では成立せず、このステップは書いたとおりには一度も回らない。`ssh-proxy.py` の既存シグナルも SIGHUP＝既存接続を全部切る / SIGTERM＝全部閉じて終了で、「既存は生かして新規だけ拒否」に相当するモードがない / `ssh-proxy.py` に「新規接続だけ拒否（既存は維持）」のシグナル（例: SIGUSR2）を足し、中継経由で `running` になってから拒否モードに入れて `MOBILE_IDE_UPLOAD_FILE` を発火させる。それを避けるなら `MOBILE_IDE_UPLOAD_PORT` のようなアップロード専用の上書きを `LaunchOptions` に足す

## P1
- `設計 > ImageUploader.swift` / `Phase 1 > 3` — 失敗を一律 `ConnectFailure.other` に包む / `SSHConnection.connect` が返す `hostKeyMismatch` まで文字列に潰れ、`TerminalScreen` が持っている「新旧指紋 + 新しい鍵を信用」の特別扱いに乗らない。ホストを入れ替えた直後にアップロードだけ意味不明なエラーになる / `ConnectFailure` はそのまま再送出し、それ以外だけ `.other` に包む
- `Phase 2 > 3` — `running` 判定が選択時の事前チェックだけ / アップロードは数秒かかり、その間に回線が揺れて `reconnecting` に落ちると `PTYSession.send` は `guard state == .running` で黙って捨てる。ファイルはホストに置かれたのに何も打ち込まれず、エラーも出ない（一番気づきにくい失敗） / アップロード完了後に再度 `running` を確認し、違えば「送信できませんでした。接続後にもう一度」＋流し込みだけ再実行できる導線（アップロード済みパスを保持）にする
- `Phase 2 > 1`（AttachmentButton の selection）— `PhotosPicker` の `selection` を処理後にクリアする記述がない / 同じ写真をもう一度選ぶと配列が前回と等しく `onChange` が発火せず、2 回目が無反応になる / ハンドラの最後で `selection = []` に戻す（`Task` 起動直後に手元へ移してクリア）
- `Phase 2 > 1`（変換の実行コンテキスト）— View の `Task` で transcode を呼ぶと MainActor 上で走る / ImageIO の縮小は 12MP × 4 枚で数百 ms 単位、その間 UI と端末描画が固まる（`onOutput` も MainActor） / `ImageTranscoder` を `nonisolated` な純関数にして `Task.detached`（または専用 actor）で回し、進捗だけ MainActor に戻す
- `調査で確定した事実 > 3` / `Phase 2 > 4` — PNG かどうかを `PhotosPickerItem.supportedContentTypes` で判定する一方、DEBUG の `MOBILE_IDE_UPLOAD_FILE` は拡張子から決めることになり分岐が二重になる / Phase 3 の自走はファイル経路しか通らないので、実際に使う picker 側の PNG 判定は Phase 4 の手動まで一度も検証されない / 判定を入力データ自身（`CGImageSourceGetType` の UTI）に一本化して `transcode(_ data:)` の中に閉じ、picker とデバッグ経路で同じコードを通す
- `設計 > ImageUploader.swift` / `Phase 1 > 3` — `createDirectory` / `getAttributes` の存在と挙動を既定として書いているが、#2 のスパイクで実際に通したのは `openSFTP` と `getRealPath(".")` だけ（スパイクの記録もその 2 つしかない） / Citadel 0.12.1 に `getAttributes` が無い・既存ディレクトリで例外の種類が違うと Phase 1 の実装が空振りする / Phase 1 の頭で API を実物で確認する項目を 1 つ足し、無ければ同じ接続の `executeCommand("/bin/mkdir -p <path>")`（exec は `.zshrc` を読まないので絶対パス）に倒す。冪等性もこちらの方が素直
- `概要・やりたいこと` / `調査で確定した事実 > 5` — 置き場所 `~/.claude/uploads/` は tmux セッションの作業ディレクトリ（`TerminalTarget.workingDirectory`）の外 / Claude Code は cwd 外の絶対パスで許可プロンプトを挟むことがあり、そうなると毎回スマホで承認を押す UX になる。`~/.claude/` は Claude Code 自身の設定ディレクトリでもある / `Phase 3 > 7` の `claude -p` を必ず**プロジェクトディレクトリを cwd にして**実行して確かめ、プロンプトが出るなら置き場所を `workingDirectory` 配下（`.mobile-ide-uploads/`、要 gitignore）に変える。`TerminalTarget` から作業ディレクトリは取れる

## P2
- `決定事項 > 2` / `Phase 3 > 3` — PNG を PNG のまま保つ理由は「UI の小さな文字を滑らせない」なのに、長辺 2048 の縮小は PNG にも掛かる（1200x2600 → 945x2048） / 縮小で滑るので目的は半分しか達成されない。スクショだけ縮小しない閾値（例: 長辺 3000 まではそのまま）を選べる余地がある
- `Phase 1 > 4` — `UploadName.make` の「重複しないこと」テストは乱数を注入する以上ほぼ形式テストにしかならない / 同一秒に 4 枚置くなら乱数より連番サフィックスの方が確実で、テストも意味を持つ
- `設計 > ImageUploader.swift` — アップロード用接続にも `SSHConnection.connectTimeout`（20 秒）が効く / 転送は 1 秒弱の想定なので、失敗時に無言で 20 秒待たされる。アップロード専用に短い締め切り（5 秒程度）を持たせてもよい

## Q
- `Phase 2 > 2`（複数枚）— 4 枚中 2 枚目で失敗したときの扱いが未定（全部やめる / 成功分だけ `@a @b ` を流し込んで残りをエラーにする） / 決めないと `ImageUploader.upload` の戻り値の形（`[String]` か部分結果 + エラー）が変わり、Phase 1 の署名から作り直しになる
- `Phase 2 > 2`（転送中のオーバーレイ）— 転送中にキャンセルできるか、オーバーレイが端末操作を塞ぐかが未定 / 決めないと、遅い回線でアプリが数十秒操作不能に見える状態を後から UI ごと足すことになる

````

**対応**: P0 2 件を反映: (1) Phase 0 を新設し、tmux 上の対話 TUI に send-keys で流し込んで `@` 補完に食われないか・cwd 外の絶対パスで許可プロンプトが出ないかを先に確かめ、送る文字列と置き場所を書き戻してから Phase 1 に入る。Phase 3 の `claude -p` 検証も実経路（対話）に変更し、決定事項側の「Phase 4 で確かめる」との食い違いを解消 (2) 失敗経路は「中継経由で running になってから、中継を『新規接続だけ拒否』モードに入れる」に変更（ssh-proxy.py にそのモードを足す。設計は実装時）。P1 は全部書き換えで反映: ConnectFailure はそのまま投げる / アップロード後に running を再確認して入力できなければパス付きでアラート / selection を空に戻す / transcode は nonisolated + Task.detached / PNG 判定は CGImageSourceGetType に一本化して preferPNG 引数を廃止 / createDirectory + getAttributes をやめて executeCommand の mkdir -p に / 置き場所は Phase 0 の許可プロンプト確認で分岐。P2: ファイル名を連番に変更（採用）。PNG の縮小閾値は dig-lite の決定（長辺 2048 で統一）に反するので見送り、plan に明記。アップロード専用の短い締め切りは「足す修正」なので見送り（終了報告に回す）。Q は両方自分で決定: 途中失敗は成功分だけ流し込んで失敗枚数をアラート（根拠: 成功したファイルはホストに残るので捨てる理由がない）/ 転送中は再接続と同じ上端バナーで端末を塞がず、キャンセルは範囲外（根拠: #7 で同じ形の UI がある）

## 2回目

````text
再読しました。前回の P0/P1 は反映されています。新しい構成で残っている問題だけ挙げます。

## P0
- `Phase 3 > 6`（失敗経路）— 「中継経由で `running` になってから中継を新規拒否モードにして `MOBILE_IDE_UPLOAD_FILE` を発火」という順序が、今の発火契機では作れない / `Phase 2 > 4` のとおり `runAutomation` は最初に `running` になった瞬間に走り、アップロードだけを遅らせる手段がない。スクリプトが `TERMINAL connected` を見てからシグナルを送るのでは数十 ms の競走になり、先にモードを入れれば端末の初回接続自体が弾かれる。ここが回らないと `UPLOAD failed` の経路（＝この機能で一番出やすいエラー）が自走で一度も踏まれない / `MOBILE_IDE_PROBE_AFTER` と同じ形で `MOBILE_IDE_UPLOAD_AFTER=<秒>`（既定 0）を `LaunchOptions` に足し、`Phase 2 > 4` を「running の N 秒後に発火」に変える。`verify-upload.py` はその窓で中継を切り替える
- `Phase 0 > 2`（許可プロンプトの確認）— 確かめる対象のパス（`~/.claude/uploads/probe.jpg`）に実ファイルを置く手順がない / 存在しないパスだと Claude Code は許可を訊く前に「ファイルがない」で終わり、「プロンプトが出なかった」という誤った結論のまま `~/.claude/uploads` で実装してしまう。Phase 0 は置き場所を決めるための Phase なので、ここを外すと Phase 4 の手動確認で初めて発覚し、置き場所・パス生成・Phase 3 の期待値をまとめてやり直すことになる / `Phase 3 > 1` の `scripts/make-test-images.sh` を Phase 0 の先頭に移し、生成した JPEG を候補パスに `scp`/`cp` してから `@` を流す。cwd 内（`~/mobile-ide/...`）にも同じ画像を置いて、プロンプトの有無が「cwd 外だから」なのか常に出るのかを切り分ける

## P1
- `設計 > ImageUploader.swift` / `Phase 1 > 3` — 同じ関数の失敗の返し方が食い違っている（設計は「`ConnectFailure` はそのまま**投げ**、SFTP の失敗だけ `.other` に包む」、Phase 1 は非 throws で `(paths:failure:)` を**返す**）/ 部分成功を返す以上シグネチャは 1 つに決まる必要があり、呼び出し側（`Phase 2 > 2` の alert 分岐、`hostKeyMismatch` の特別扱い）の書き方が変わる / タプルを返す側に寄せて設計ブロックの文を直す（`ConnectFailure` は包まずそのまま `failure` に入れる、と書く）
- `Phase 3 > 2`〜`Phase 3 > 4` / `Phase 3 > 7` — 前半は `MOBILE_IDE_TERMINAL_AUTORUN=1`（`TerminalTarget.mobileIDE` 固定 = tmux `mobile-ide`）なのに、step 7 は「tmux セッションは `form` を使う」と書いてあり矛盾している / VERIFY.md に「`mobile-ide` を使うと実機 iPhone が attach していると `-D` で蹴り合って判定が狂う」実例つきで書いてあるとおりで、`capture-pane -t mobile-ide` の確認は実機を触っている最中に不安定になる。流し込んだ文字列が自分の作業セッションに入る副作用もある / 前半も `MOBILE_IDE_OPEN_PROJECT=form`（`verify-reconnect.py` と同じ）に揃え、`capture-pane -t form` にする
- `Phase 2 > 4` / `Phase 3` 全体 — `MOBILE_IDE_UPLOAD_FILE` が 1 枚しか取れないため、複数枚に関する決定（`決定事項 > 3` の部分失敗、`決定事項 > 4` のバナー `n / N`、`@a @b ` の連結順）が自走では一度も検証されず、`Phase 4 > 3` の手動頼みになる / 実機の手動確認で初めて壊れが出ると、`ImageUploader` の戻り値と画面の両方に手が入る / カンマ区切りで複数パスを受けるようにし、`Phase 3` に「2 枚（jpg + png）で `UPLOAD start n=2` → `@a @b ` の順に typed、バナーが 1/2 → 2/2」の項目を足す。壊れたデータのファイルを 1 枚混ぜれば部分失敗も同じ経路で回せる
- `決定事項 > 3` / `設計`（目印行）— 部分失敗の決定はアップロード失敗だけを想定していて、`ImageTranscoder` が N 枚中 1 枚で `undecodable` を投げたときの扱いが未定義。目印行にも部分成功を表す形がない（`UPLOAD done` と `UPLOAD failed` の二択） / 実装時に「変換で落ちた 1 枚をどう数えるか」を場当たりで決めることになり、自走の判定条件も書けない / 変換失敗も同じ「成功分だけ流し込み、失敗枚数をアラート」に含めると明記し、目印行を `UPLOAD done n=<成功> failed=<失敗>` の形にする

## P2
- `Phase 2 > 3` — 転送後に `running` でなかったときのアラートがパスの表示止まりで、再接続後に流し込み直す導線がない / 実際には数十秒で再接続が戻るので、「再接続を待って入力」ボタン（成功パスを保持して `send` するだけ）があると手打ちを避けられる。今回の範囲外にするなら plan に一行書いておくと後で迷わない
- `Phase 0 > 3`（書き戻し）— 書き戻す先が「調査で確定した事実」と「設計」だけになっているが、`Phase 3 > 2`〜`5` と `Phase 4 > 2` には `/Users/d0ne1s/.claude/uploads/` が直書きされている / 置き場所が変わったときに期待値だけ古いまま残る。書き戻し対象に Phase 3 / 4 の期待パスも含める
- `設計`（ファイル名）— `yyyyMMdd-HHmmss-<3 桁ミリ秒>-<連番>` はミリ秒と連番が二重（同一バッチは連番、別バッチは秒で分かれる）/ どちらか一方で足り、`UploadName` のテストも短くなる

## Q
- `Phase 0 > 2` — 許可プロンプトが出た場合の逃げ道が「プロジェクト配下に置く（要 gitignore）」の一択で書かれているが、ホスト側の `~/.claude/settings.json` の permissions に `~/.claude/uploads` の Read を一度足す選択肢もある / どちらを採るかで、リポジトリを汚すか・ホスト設定に依存する（新しい Mac mini でセットアップが要る）かが変わる。Phase 0 の結果が出てから決めるにしても、判断軸を先に決めておかないと Phase 1 の置き場所の実装が止まる

````

**対応**: P0 2 件を反映: (1) `MOBILE_IDE_UPLOAD_AFTER=<秒>` を足し、失敗経路は running を見てから中継を拒否モードにして 5 秒後に発火する形に (2) テスト画像の生成を Phase 0 の先頭に移し、cwd 外（`~/.claude/uploads/probe.jpg`）と cwd 内（`~/mobile-ide/.probe.jpg`）の両方に実ファイルを置いてから `@` を流す。P1 は全部書き換えで反映: ImageUploader は非 throws でタプルを返す形に設計ブロックを統一 / Phase 3 を `MOBILE_IDE_OPEN_PROJECT=form` に揃えて `capture-pane -t form` に / `MOBILE_IDE_UPLOAD_FILE` をカンマ区切りの複数枚にし、jpg + png の 2 枚と壊れたファイル混在の部分失敗を Phase 3 の既存項目の書き換えで検証 / 変換失敗も部分失敗に含めると明記し、目印行を `UPLOAD done n=<成功> failed=<失敗>` に。P2: 再送ボタンは範囲外と明記（採用） / Phase 0 の書き戻し先に Phase 3・4 の期待パスを含めた（採用） / ファイル名からミリ秒を落として秒 + 連番に（採用）。Q は自分で決定: 許可プロンプトが出たら dotfiles 管理の `~/.claude/settings.json`（symlink を確認済み）に `~/.claude/uploads/**` の Read 許可を足す。Mac mini にも dotfiles で載り、リポジトリを汚さない

## 3回目

````text
再読しました。前回の P0 2 件・P1 4 件はいずれも反映されていて、失敗経路の順序（`MOBILE_IDE_UPLOAD_AFTER`）と Phase 0 の実ファイル配置は成立する形になっています。残りは検証の詰めと、選定理由が崩れる 1 点です。

## P0

## P1
- `設計`（目印行）/ `Phase 3 > 2` — `UPLOAD start n=` と `UPLOAD done n= failed=` を誰が出すか、変換失敗をどちらの数に入れるかが決まっていない / `決定事項 > 3` で変換失敗も部分失敗に含めた結果、`ImageUploader`（アップロードしか知らない）では `failed=1` を出せない。素直に実装すると `ImageUploader` が目印行を出し、壊れた 1 枚は数に現れず `Phase 3 > 2` の期待値（`UPLOAD done n=1 failed=1`）が最初から合わない / 目印行は「変換 + アップロードをまとめる呼び出し側」が出すと明記し、`start n=` は選択枚数、`done` は変換失敗と転送失敗を合算した数、と決めておく。アラートの「1 枚送れませんでした」もこの合算値を使う
- `Phase 3 > 1`（バナーの判定）— 「バナーは 1 / 2 → 2 / 2（目印行で判定）」と書いてあるが、進捗を表す目印行が存在しない（`start` / `put` / `done` / `failed` / `typed` のみ）/ `put` は `ImageUploader` 側の出力で、`TerminalScreen` の `uploadProgress` が実際に更新されたかは何も示さない。バナーの配線が壊れていても自走は緑のまま通り、Phase 4 の手動で初めて気づく / 進捗コールバックを受けて `uploadProgress` を更新する側で `UPLOAD progress <i>/<N>` を出し、それを判定に使う
- `決定事項 > 4` / `Phase 0 > 3` — 逃げ道が `settings.json` の Read 許可の追加と決め打ちされているが、cwd 外のファイルはツール権限とは別の「作業ディレクトリの境界」の話で、`permissions.additionalDirectories`（`/add-dir` 相当）でないと通らない可能性が高い / 想定した書き方で効かないと Phase 0 の再確認で足踏みし、置き場所の判断も宙に浮く / Phase 0 の再確認で `permissions.allow` の `Read(...)`（`~` 展開の癖を含む）と `additionalDirectories` の両方を試し、効いた方を書式ごと決定事項に書き戻す
- `Phase 0 > 3` / `Phase 5` — `~/.claude/settings.json` は dotfiles リポジトリへの symlink なので、Phase 0 で編集すると**別リポジトリ**に未コミットの差分が残る。その扱いがどの Phase にも無い / コミットされなければ Mac mini には載らず、「dotfiles で載るのでホスト固有の手作業にならない」という選定理由が成立しない。このリポジトリのコミット（Phase 5）にも現れないので、後から追跡もできない / Phase 0 に「dotfiles 側の差分を人間が確認してコミットする」項目（[人間👨‍💻]）を足し、Phase 5 の記録にも設定変更をした事実を残す

## P2
- `Phase 3 > 1` — `ls -l ~/.claude/uploads/` に「2 ファイル」を期待しているが、Phase 0 の probe.jpg や前回実行の残骸が同じディレクトリにある / 枚数ではなく `UPLOAD put` が出したパスで照合する方針を `verify-upload.py` に書いておくと、実行順に依存しない
- `Phase 3 > 4` — 「`~/.claude/uploads/` を消してから起動」は Phase 0 の probe.jpg と、実機で上げた画像も巻き添えにする / `verify-upload.py` が消す対象を専用サブディレクトリ（例: `uploads/` ごとではなくテスト用の一時ディレクトリ）に寄せるか、消す前に退避すると後続の手動確認とぶつからない
- `Phase 2 > 3` — `LaunchOptions` の先頭 doc コメントは全環境変数の一覧になっているので、`MOBILE_IDE_UPLOAD_FILE` / `MOBILE_IDE_UPLOAD_AFTER`（DEBUG 限定にするかも含めて）をそこに足す一文を入れておくとよい

## Q

````

**対応**: P0 なし → 収束。P1 は全部反映（未レビュー）: 目印行は呼び出し側が出し `done` は変換失敗 + 転送失敗の合算と明記 / `UPLOAD progress <i>/<N>` を uploadProgress 更新側が出してバナーの判定に使う / 許可の書式は `permissions.allow` の Read と `additionalDirectories` を Phase 0 で両方試して書き戻す形に / Phase 0 に「dotfiles 側の差分を人間がコミット」を追加し Phase 5 のログにも残す。P2: `ls` の枚数でなく `UPLOAD put` のパスで照合（採用）/ `~/.claude/uploads/` は消さず `mv` で退避して戻す（採用）/ LaunchOptions の doc コメント追記は実装時に当然やることなので plan には書かない（見送り）
