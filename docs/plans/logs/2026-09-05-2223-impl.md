review session: ad038843-513c-49fe-bdf6-59331bcda9c8

## 1回目

````text
未コミット差分（Swift 8 ファイル + テスト + 検証スクリプト + ドキュメント）と plan `docs/plans/2026-09-05-2120-review-fixes.md` を読みました。リーク修正と DECCKM の撤去は筋が通っていて、`mise run test` / `verify-reconnect.py` は環境の都合でこちらでは実行していません（判断はコード読解とログ記載に基づく）。

## P0

## P1
- `MobileIDE/Core/SSH/PTYSession.swift:verifyAlive()` — `backgroundedAt` を「誰が最初に呼んでも」入口で読み捨てるので、**バックグラウンド中に `network.onChange` が発火すると記録が経過 0 秒として消費される** / plan が想定したのは「復帰後にどちらが先に呼ぶか不定」だけだが、`NetworkPathObserver` はバックグラウンド実行猶予の間も生きている（`TerminalScreen` は `onDisappear` でしか `network.stop()` しない）。ロック直後に Wi-Fi が落ちて経路キーが変わる → `onChange` → `verifyAlive()` で記録が消え、さらに `probing = true` のまま suspend する。5 分後の `.active` では記録が nil で `guard !probing` に弾かれ、**この修正が一番効いてほしい経路（無音の死）で従来どおり probe 待ちに戻る** / `enterBackground()` で立てたフラグを `.active` のハンドラでだけ下ろし、`verifyAlive()` は「フォアグラウンドにいる」ときだけ `backgroundedAt` を消費・判定する（`isBackground == true` の間は読み捨てない）。復帰後に `onChange` が先行するケースは今のまま効く
- `MobileIDE/Features/Terminal/TerminalScreen.swift:body`（`onDisappear`） — `surface.tearDown()` の直後に `wired = false` を置いている / `tearDown()` は不可逆（`updateUiClosed()` に対になる再開 API が無いことは plan の前提に明記）なのに、`wired = false` は `wireUp()` の `guard !wired` を通してしまう唯一の経路を自分で開けている。再 `onAppear` が来たら `TERMINAL surface reused after tearDown` が 1 行出るだけで、画面は描画されない端末として黙って壊れる（plan の「無言で壊れないように」の意図と食い違う） / `wired = false` を落とす（`@State` の箱ごと捨てられるので意味がない）。残すなら `wireUp()` の guard を `guard !wired, !surface.isTornDown` にして、torn down なら surface を作り直す
- `scripts/verify-reconnect.py:シナリオ 7 / 8` — `MOBILE_IDE_RESUME_AFTER` は `runAutomation` から `session.enterBackground(at:)` を直接呼ぶので、**`onChange(of: scenePhase)` の `.background` → `enterBackground()` という本番唯一の記録経路が 13 項目のどれでも通らない** / `.background` のケースを消しても・書き間違えても全 PASS する。この配線を見る手段は Phase 4（未実施）だけで、その Phase 4 の期待も「探らず張り直す」に書き換え済み / シナリオ 7 の PASS 条件に `TERMINAL background` が出ていることを足す（シミュレータでも `simctl` でホームに戻せば `.background` は起こせる）か、少なくとも「配線は実機でしか見ていない」ことを plan のログに明記して Phase 4 を必須にする

## P2
- `MobileIDE/Core/SSH/PTYSession.swift:deinit` / `MobileIDE/Core/Terminal/SwiftTermSurface.swift:deinit` / `TerminalScreen.wireUp()` — 検証専用の目印行（ヒープのポインタ入り）が `#if DEBUG` の外にある / 他の DEBUG 限定の仕掛け（`HOME settings …`、`MOBILE_IDE_*`）は揃って gate されているので流儀が揃わない。`deinit` は main 以外で走りうるので、そこでの `print` + `fflush` も DEBUG に閉じたほうが素直 / `#if DEBUG` で包む
- `MobileIDE/Core/Terminal/TerminalSurface.swift:debugObjectID(_:)` — 端末 protocol のファイルに、SSH 層（`PTYSession`）も使う汎用ヘルパが置かれている / 依存の向きが読みにくい / `Core/App` 側のデバッグ用ファイルに移す
- `MobileIDE/Core/Terminal/SwiftTermSurface.swift:tearDown()` — `isTornDown` は `onInput` の didSet でしか見ていないので、`feed` / `send` / `showKeyboard` は tearDown 後も黙って通る / 「以後 feed も再開もできない」という doc の契約が実行時には無検査 / DEBUG で `feed` にも同じ目印行を出すか、`isTornDown` を `private(set)` で公開して呼び側の guard に使う
- `scripts/verify-reconnect.py:6a` — `all(app.has(f"surface deinit {i}") or app.has(f"session deinit {i}") ...)` の or は、surface の id が `session deinit` 行に出ても通る書き方 / 実際には取り違えは起きないが、意図（surface id は surface 行、session id は session 行）が読み取れない / `ids` を surface / session で分けて別々に検査する。ついでに `TERMINAL surface reused after tearDown` が出ていないことも足すと、上の P1（`wired = false`）の回帰が拾える
- `MobileIDE/Features/Terminal/TerminalScreen.swift:body`（DEBUG の `view released`） — 2 秒固定の `Task.sleep` で解放を判定している / 解放が遅れた回はグレーになる（plan でも 6 秒に伸ばして見直した経緯がある） / 0.5 秒間隔で最大 5 秒ポーリングして「解放された時点」を出す

## Q
- `docs/plans/2026-09-05-2120-review-fixes.md:Phase 3 前の計測 / Phase 4` — 「復帰のたびに 3 秒払っているか」の実機計測（Phase 3 の実施ゲート）を飛ばして実装まで入れた。plan のログでも「計測で probe 経路が出ないと分かったら、残すか外すかをそのとき決める」と先送りされたまま / 計測して probe 経路が出なければ `staleAfterBackground` は無害だが不要な複雑さ（+ 起動オプション 1 つ + シナリオ 2 本）として残り続ける。コミット前に測るか、入れたままコミットして Phase 4 で判断するかを決める必要がある
- `MobileIDE/Core/SSH/ReconnectPolicy.swift:staleAfterBackground` — 60 秒超の復帰では**生きている接続でも必ず**張り直す設計にしたが、attach は `tmux new-session -A -D` なので、Mac 側が同じ tmux セッションに attach していると iPhone を開くたびに Mac が detach される（VERIFY.md が `mobile-ide` で実際に起きた例として記録している状況） / plan の「生きていても attach し直すだけで害はない」は iPhone 側だけを見た判断で、Mac 側の巻き添えは織り込まれていない。許容するのか、Mac と共有するセッションでは条件を変える（閾値を上げる / `-D` を外す）のかを決めないと、日常の使い勝手として効いてくる

````

**対応**: P1: (1) PTYSession に inBackground フラグを足し、enterBackground() で立て、.active の enterForeground()（実経過秒を TERMINAL resume background=Ns に出す）で下ろす。verifyAlive() は inBackground 中は何もしない（探らず記録も消費しない）。RESUME_AFTER の自走も background → foreground → verifyAlive の順に。(2) onDisappear の wired = false を削除（tearDown は不可逆）。onInput の didSet による再利用検出は消し、代わりに isTornDown を protocol に private(set) で公開して feed を捨てる（TERMINAL surface fed after tearDown）。(3) .background 配線がシナリオ 7/8 で通らない件は、シナリオの追加はループ中は見送り（終了報告で提案）、plan の方針変更に「実機 Phase 4 で必ず見る」を明記。P2: deinit / wired の目印行を #if DEBUG に閉じた／debugObjectID を LaunchOptions.objectID に移した／feed を tearDown 後に捨てるようにした（上記）／6a は surface と session の id を分けて検査し、after tearDown の行が無いことも条件に足した／view released は 0.5 秒間隔で最大 5 秒ポーリングして after=Ns を出す。Q: 2 件とも自分で決めて plan に記載。実機計測を飛ばして実装を入れたまま Phase 4 で判断（根拠: シナリオ 3 の probe 3.1 秒の実測）／60 秒超の復帰で Mac 側が detach されるのは許容（-D は開くたび・再接続のたびに既に効いており、共有セッションは VERIFY.md が避けるよう書いている）。

## 2回目

````text
再レビューしました。前回の P1 3 件・P2 5 件はいずれも差分に入っており、`verifyAlive()` の `inBackground` ガード、`wired = false` の削除、`isTornDown` による feed 破棄、`LaunchOptions.objectID` への集約、6a の id 分離検査は意図どおりです（`mise run test` / `verify-reconnect.py` は環境の都合でこちらでは実行せず、コード読解での判断）。

## P0

## P1
- `docs/plans/2026-09-05-2120-review-fixes.md:Phase 1 > ステップ 10`（`scripts/verify-reconnect.py にシナリオ 6 を足す`） — **実装済みなのにチェックボックスが `[ ]` のまま、かつ本文が古い**（PASS 条件が `view released=true`、件数が「6 シナリオ 11 項目」。実際は `previous=true` / 8 シナリオ 13 項目） / Phase 5 ステップ 3「plan のチェックボックス・ログ」は `[x]` になっているので、plan だけ読むと「シナリオ 6 は未着手」と読める。次の再開時に二重実装か取りこぼしのどちらかを起こす。同じズレが Phase 3 > ステップ 1（`.active` は今のまま `verifyAlive()`）と ステップ 6（入口で `backgroundedAt` をクリア、だけ）にもあり、今回入れた `enterForeground()` / `inBackground` がステップ本文に反映されていない（「決定」節 line 45 にはある） / ステップ 10 に `[x]` を付けて条件を `previous=true` / 8 シナリオ 13 項目に直し、Phase 3 のステップ 1・6 に `enterForeground()` と `inBackground` ガードを 1 句ずつ足す
- `MobileIDE/Core/SSH/PTYSession.swift:enterForeground()`（Phase 3 > ステップ 6） — `inBackground` を下ろすだけで `verifyAlive()` は呼び出し側任せになっている（`TerminalScreen` の scenePhase `.active` と `runAutomation` の 2 か所で対にしている） / 対を崩すと `backgroundedAt` が残ったまま `inBackground == false` になり、その後の任意の `network.onChange` が何分も前の記録で**生きている接続を切る**。これは plan「前提 §3」line 41 が名指しで警戒している壊れ方そのもので、しかも「復帰の瞬間しか起きない」ので自走シナリオでは出ない。呼び忘れを型で防げるのに規約に頼っている / `enterForeground()` の末尾で `verifyAlive()` を呼ぶ（または `resumeFromBackground()` 1 本にまとめる）。呼び出し側は 2 か所とも 1 行になり、`TERMINAL resume background=Ns` → 判定の順序も 1 か所に閉じる
- `VERIFY.md:再接続（#7）節` / `MobileIDE/Core/App/LaunchOptions.swift:probeAfter`（Phase 5 > ステップ 1） — 「scenePhase はシミュレータから起こせないので `MOBILE_IDE_PROBE_AFTER` / `MOBILE_IDE_RESUME_AFTER`（DEBUG）で同じ経路を呼ぶ」と断定で書き直したが、plan の方針変更（line 132）に**`simctl launch` で背面に回せば `TERMINAL background` → `foreground` → `resume background=2s` → `probe ok` が出ることを実測済み**と記録されている。plan「前提 §3」line 43 も同じ古い前提のまま / この plan 自体が「誤った前提のコメントが 1 日で 20 行のコードを生んだ」件（Phase 5 の `/retro` 対象）の後始末なのに、同じ形の反証済みの断定を 3 か所に残すことになる。次に読む人がシナリオ 9 を「原理的に無理」と諦める / 「起動オプションのほうが速くて安定するので自走では使わない。背面に回す経路自体は `simctl launch` で再現できる（シナリオ 9 の課題）」に書き換える。`LaunchOptions` の `MOBILE_IDE_PROBE_AFTER` の理由書きと plan line 43 も同じ扱いに

## P2
- `MobileIDE/Features/Terminal/TerminalScreen.swift:body`（`onDisappear` の DEBUG ブロック、Phase 1 > ステップ 3） — ポーリングの継続条件が `view != nil || (hasPrevious && previous != nil)` なので、UIKit が握り続ける `current` が常に非 nil → **どの回も 5.0 秒フル待ちして `after=5.0s` しか出ない**（1 回目も 2 回目も）。判定したい値（`hasPrevious` の回は `previous`、初回は `view`）だけを見て抜ければ「解放された時点」が出る。close あたりの待ちも 2 秒に戻る
- `MobileIDE/Core/Terminal/TerminalSurface.swift:isTornDown` — protocol に公開したが読み手が `SwiftTermSurface.feed` の内部だけで、外部利用者がゼロ / 差し替え実装への契約表明としては `feed` の doc コメントで足りる。`private` に戻して protocol は `tearDown()` だけに保つほうが口が狭い
- `VERIFY.md:実機（再接続）` — 復帰時の期待を `TERMINAL background` → `foreground` → `lost stale after background Ns` → `connected` と書いているが、実際は `foreground` と `lost` の間に `TERMINAL resume background=Ns` が入る / Phase 4 はこの行の並びで判定するので、実際に出る行を並べておく（`resume background=Ns` は経過秒の答え合わせにも使える）
- `docs/plans/2026-09-05-2120-review-fixes.md:Phase 2 > ステップ 5` — 「`mise run test`（4 件とも通る）」だが `SwiftTermSurfaceTests` は 3 メソッド（on→off を 1 件にまとめた）。ログ line 126 は「3 件中」と正しい / ステップ側を 3 件に直す

## Q

````

**対応**: P0 なしで収束。P1/P2 はすべて書き換えで済むので採用: plan の Phase 1 ステップ 10 を [x] にして条件（previous=true・件数）を直し、Phase 3 ステップ 1・6 に enterForeground() / inBackground を追記、Phase 2 ステップ 5 を 3 件に／enterForeground() の末尾で verifyAlive() を呼び、呼び出し側（scenePhase .active と runAutomation）は enterForeground() の 1 行に／VERIFY.md・LaunchOptions・plan 前提 §3 の「scenePhase は起こせない」を「起動オプションのほうが速くて安定するので自走ではこちら。背面に回す経路は simctl launch で再現できる（シナリオ化は未着手）」に書き換え／view released のポーリングは初回は current・2 回目以降は previous だけを見る／isTornDown を private に戻し protocol は tearDown() だけ／VERIFY.md 実機の期待行に resume background=Ns を追加。見送りなし。
