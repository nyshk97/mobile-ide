review session: 8262ce11-1a8c-4809-b066-0b12f0c1d26e

## 1回目

````text
（`mise run test` / `xcodebuild` はこの環境で許可が下りず未実行。以下は静的レビュー）

## P0

## P1
- `MobileIDE/Features/Terminal/TerminalTarget.swift:mouseSetup` — 既定バインドの `select-pane -t=` から `-t=` が落ちている / `-t=` はマウスイベントが起きたペインを指す指定で、これが無いと複数ペインのとき「指の下でないペイン」が copy-mode でスクロールする（tmux 既定からの退行。デスクトップ側で分割したセッションにアプリが attach したときに出る） / `select-pane -t= '\;' send-keys -X -N 1 scroll-up` に戻す。
- `MobileIDE/Features/Terminal/TerminalTarget.swift:mouseSetup` — `-T copy-mode` にしか bind していない / tmux は `mode-keys` が vi のとき copy-mode 中のキーテーブルが `copy-mode-vi` になり（`mode-keys` は `VISUAL`/`EDITOR` に `vi` が含まれると自動で vi になる）、その環境では `-N 1` がまったく効かず既定の 5 行のまま = 指 1 行で 5 行飛ぶ。Air で OK だったのは Air が emacs モードだったからで、Mac mini 到着後に同じ dotfiles でも環境変数次第で崩れる / 同じ 2 行を `-T copy-mode-vi` にも流す（4 コマンドにする）。
- `MobileIDE/Features/Terminal/TerminalTarget.swift:mouseSetup` — `set-option mouse on` はセッションオプションなのでアプリが detach した後も残る / plan が dotfiles を避けた理由は「デスクトップ側の tmux のドラッグ選択を変えないため」だが、`mobile-ide` のようにユーザーが Mac からも attach するセッションでは、アプリが一度開いた時点でデスクトップ側の挙動も変わる（この意図と食い違う） / `set-hook -t <session> client-detached 'set -t <session> mouse off'` を併せて流して離脱時に戻すか、戻さないなら README / VERIFY.md に「アプリが開いたセッションは Mac から attach しても mouse on」と明記する。
- `MobileIDE/Features/Terminal/TerminalTarget.swift:mouseSetup` — `bind-key` はセッションではなく tmux サーバー全体・かつ永続で、アプリが接続するたびに上書きする / コメントの「`mouse` が off の他セッションには効かない」は「今は発火しない」だけで、ユーザーが他セッションで mouse を on にした瞬間から、あるいは自前の copy-mode ホイールバインドを持っていた場合はサーバー再起動まで奪われる / コメントを実態（サーバー全体を恒久的に書き換える）に直し、README のトレードオフ記述に 1 行足す。
- `MobileIDE/Features/Terminal/TerminalTarget.swift:launchCommand` — tmux コマンド列を組み込んだ後の end-to-end 実行記録が plan のログに無い（実機で確認したのは手で `set -t mobile-ide mouse on` した状態、その後の実機ログは SwiftTerm 側の `TERMINAL mouse on` のみ） / `set-option` が想定どおり attach 先セッションに当たるか、bind の `'\;'` がログインシェル → tmux argv を通って区切りとして届くかは実行しないと確定しない。外れると attach 直後の画面に tmux のエラーが出る / VERIFY.md に自分で書いた `show -t mobile-ide -v mouse` が `on`・`list-keys -T copy-mode` が `-N 1` を、`form` セッションで 1 回通してからコミットする。

## P2
- `MobileIDE/Core/Terminal/WheelScrollingTerminalView.swift:cellHeight` — SwiftTerm の `computeFontDimensions` を式ごと写している（`cellWidth` も同様） / 実際のセル寸法とずれると行が下に行くほど狂い、ホイール位置とクリック位置が静かにずれる（ビルドは通る） / `WheelScrollingTerminalViewTests` に `Int(view.bounds.height / view.cellHeight) == view.getTerminal().rows` 相当の整合アサーションを 1 本足して、ずれたらテストで落ちるようにする。
- `MobileIDE/Core/Terminal/WheelScrollingTerminalView.swift:reportWheel` — 上限で送らなかった分まで `pendingDistance -= CGFloat(wanted) * cellHeight` で消費している / 速いフリックで指の移動量より少なくしかスクロールしない（upstream のトークンバケットも捨てる設計なので意図なら妥当だが、コードからは読めない） / `lines` 分だけ引いて残りを次イベントに持ち越すか、捨てるのが意図ならコメントに書く。
- `MobileIDE/Core/Terminal/WheelScrollingTerminalView.swift:handleWheelPan` — `.began` で `setTranslation(.zero)` していない / 認識までのスロップ（約 10pt ≒ 0.7 セル）が最初の `.changed` に乗り、ドラッグ開始直後に 1 行余計に送ることがある / `.began` でも translation を 0 にする。
- `MobileIDE/Core/Terminal/WheelScrollingTerminalView.swift:screenCell` — y だけ `contentOffset` を引き、x は引いていない / 横スクロールが起きない前提に依存しているが、その前提がコードにもコメントにも無い / 両方引くか「contentSize.width == bounds.width なので x は素通し」と書く。
- `README.md`（端末エミュレータの項） — 「二本指を従来スクロール」と書いたが、tmux クライアントは常に代替画面で描くので外側にスクロールバックが無く、実際に二本指で見えるものは無い / 読んだ人（未来の自分）が退行と誤認する / 「二本指は SwiftTerm 側の従来動作に残す（tmux 越しでは見えるスクロールバックが無い）」と補う。

## Q
- `docs/plans/2026-09-05-2343-swiftterm-scroll-in-mouse-mode.md:Phase 2` — 二本指の実機確認（素のシェルでスクロールバック / Claude Code 内で選択が出ない）が未了で、Phase 3 の「実機結果の転記」も空のまま / このままコミットすると、plan 上は未検証の状態で「完了」の記録だけが残る。先に実機で二本指を見るか、二本指は合否にしないと決めて Phase 2 を閉じるかは人が決める必要がある。

````

**対応**: P0 なしで収束。P1（`-t=` 欠落）: 見送り。tmux 3.7c の copy-mode テーブルの既定は `select-pane \; send-keys -X -N 5 scroll-up` で `-t=` は付いていない（`list-keys -T copy-mode` の実出力。`-t =` が付くのは root テーブル側）。P1（copy-mode-vi）: 採用。同じ 2 行を `-T copy-mode-vi` にも流し、テストの期待値を更新。**Air の `mode-keys` は vi だった**ので、実機で試したときの copy-mode のホイールは既定の 5 行刻みのままだった（指摘が当たっていた。1 行刻みは実機で再確認が要る）。P1（mouse がセッションに残る）: hook で戻すのは見送り（複雑さに対して得るものが小さい）。コメント・README・VERIFY.md・plan の方針変更に「アプリが開いたセッションは Mac から attach しても mouse on」と明記。P1（bind はサーバー全体・恒久）: コメントと README を実態に直した。P1（end-to-end の記録）: `form` で通した結果（`set -u` で消してから attach → mouse on / N 1 / global off / 画面にエラー無し）を plan のログに記録。P2（cell 寸法の整合）: `testCellDimensionsMatchSwiftTermGrid` を追加（bounds / cellHeight が rows、bounds / cellWidth が cols）。P2（上限で消費）: 意図（フリックの残りを持ち越さない）をコメントに書いた。P2（.began の translation）: `setTranslation(.zero)` を追加。P2（x の contentOffset）: 両軸引くようにした。P2（README の二本指）: 「tmux 越しでは見えるスクロールバックが無い」と補った。Q（二本指の実機確認）: 合否にしない項目として閉じ、Phase 2 を完了に。実機結果（一本指・タップ・tmux mouse on）を plan のログに転記。
